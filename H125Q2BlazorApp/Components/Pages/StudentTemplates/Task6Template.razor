@page "/student/6"
@rendermode InteractiveServer
@inject H125Q2BlazorApp.Services.TaskService TaskService
@inject NavigationManager NavigationManager

@* 
    INSTRUKTION TIL ELEVEN:
    ======================
    Dette er din skabelon til at implementere opgave 6: Tilfældig Dyrelyd.
    
    Du skal lave en sjov komponent, der viser et tilfældigt dyr og dets lyd
    når man klikker på en knap. Komponenten skal vælge et tilfældigt dyr 
    fra en liste og vise den tilhørende lyd.
    
    Følg opgavebeskrivelsen og hints nedenfor.
*@

<PageTitle>Opgave 6: Tilfældig Dyrelyd</PageTitle>

<h1>Opgave 6: Tilfældig Dyrelyd</h1>

@{
    var task = TaskService.GetTaskById(6);
    if (task != null)
    {
        <div class="alert alert-info">
            <h4>Opgavebeskrivelse:</h4>
            <p>@task.Description</p>
            
            <h5 class="mt-3">Hints:</h5>
            <div class="mt-2">
                <HintRenderer Hints="@task.Hints" />
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-warning">
            <p>Opgave 6 er ikke defineret i TaskService endnu. Tilføj den først i TaskService.cs.</p>
        </div>
    }
}

@* 
    DIN OPGAVELØSNING:
    ==================
    Tilføj HTML-markup herunder, f.eks. en knap til at vælge et tilfældigt dyr,
    og et område der viser det valgte dyr og dets lyd.
    Tilføj også eventvariabler og funktioner i @code-sektionen.
*@



<div class="mt-4">
    <button class="btn btn-primary" @onclick="GoBack">Tilbage til opgavedetaljer</button>
</div>

@code {
    // Erklær dine variabler her, f.eks.:
    // private List<string> dyr = new List<string> { "Kat", "Hund", "Ko", "And", "Gris" };
    // private List<string> lyde = new List<string> { "Miav", "Vov", "Muh", "Rap", "Øf" };
    // private string valgtDyr = "";
    // private string valgtLyd = "";
    // private Random random = new Random();
    
    // Alternativt kan du bruge en Dictionary:
    // private Dictionary<string, string> dyreLyde = new Dictionary<string, string>
    // {
    //     { "Kat", "Miav" },
    //     { "Hund", "Vov" },
    //     { "Ko", "Muh" },
    //     { "And", "Rap" },
    //     { "Gris", "Øf" }
    // };
    
    // Tilføj metoder her, f.eks.:
    // private void VælgTilfældigtDyr()
    // {
    //     int index = random.Next(dyr.Count);
    //     valgtDyr = dyr[index];
    //     valgtLyd = lyde[index];
    // }
    
    // Metode til at navigere tilbage til opgavedetaljer
    private void GoBack()
    {
        NavigationManager.NavigateTo("/task/6");
    }
} 