@page "/student/3"
@rendermode InteractiveServer
@inject H125Q2BlazorApp.Services.TaskService TaskService
@inject NavigationManager NavigationManager

@* 
    INSTRUKTION TIL ELEVEN:
    ======================
    Dette er din skabelon til at implementere opgave 3: Skift Baggrundsfarve.
    
    Du skal lave en komponent med en knap, der ændrer baggrundsfarven på komponenten, 
    hver gang man klikker. Farven kan skifte mellem et sæt forudbestemte farver eller 
    til en tilfældig farve.
    
    Følg opgavebeskrivelsen og hints nedenfor.
*@

<PageTitle>Opgave 3: Skift Baggrundsfarve</PageTitle>

<h1>Opgave 3: Skift Baggrundsfarve</h1>

@{
    var task = TaskService.GetTaskById(3);
    if (task != null)
    {
        <div class="alert alert-info">
            <h4>Opgavebeskrivelse:</h4>
            <p>@task.Description</p>
            
            <h5 class="mt-3">Hints:</h5>
            <pre>@task.Hints</pre>
        </div>
    }
    else
    {
        <div class="alert alert-warning">
            <p>Opgave 3 er ikke defineret i TaskService endnu. Tilføj den først i TaskService.cs.</p>
        </div>
    }
}

@* 
    DIN OPGAVELØSNING:
    ==================
    Tilføj HTML-markup herunder, f.eks. en knap og et område der ændrer baggrundsfarve.
    Tilføj også eventvariabler og funktioner i @code-sektionen.
*@



<div class="mt-4">
    <button class="btn btn-primary" @onclick="GoBack">Tilbage til opgavedetaljer</button>
</div>

@code {
    // Erklær dine variabler her, f.eks.:
    // private string aktuelFarve = "#ffffff"; // Start med hvid
    // private List<string> farver = new List<string> { "#ff0000", "#00ff00", "#0000ff", "#ffff00" }; // Rød, grøn, blå, gul
    // private int farveIndeks = 0;
    
    // Tilføj metoder her, f.eks.:
    // private void SkiftFarve()
    // {
    //     farveIndeks = (farveIndeks + 1) % farver.Count; // Gå til næste farve i listen
    //     aktuelFarve = farver[farveIndeks];
    // }
    
    // Metode til at navigere tilbage til opgavedetaljer
    private void GoBack()
    {
        NavigationManager.NavigateTo("/task/3");
    }
} 